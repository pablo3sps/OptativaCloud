@model IEnumerable<AplicacioOptativa.Models.Autos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Anio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Anio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AutosId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AutosId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AutosId })
        </td>
    </tr>
}

</table>
